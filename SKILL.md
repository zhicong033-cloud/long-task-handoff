---
name: long-task-handoff
description: 长任务对话变长时，写一份交接文档并建议开新会话继续。当一段开发/研究/写作任务的对话已经进行了很多轮（大约 30 轮以上）、或者用户提到「上下文太长」「会话太久」「Token 烧得快」「要不要重开」「续接之前的工作」「交接」「handoff」「接着上次继续」时，都要用这个 skill。也在用户明确要求写交接文档、工作交接、进度快照、handoff.md 时使用。即使用户没提到 Token 或成本，只要一段任务性对话明显拖长了，也应该主动提议。不要用于闲聊、单次问答，或用户只是想要一份普通的会议纪要/项目总结。
---

# 长任务交接与重开

## 为什么值得做

LLM 对话的成本模型是：**每一轮都把全部历史重发一遍**。所以一条会话的总成本
大致正比于轮次的平方，而不是轮次本身。

一个真实账户的实测数据（Codex，7,577 次请求）：

| 轮次 | 该轮平均携带的上下文 |
| --- | ---: |
| 第 1–10 轮 | 41,430 |
| 第 11–30 轮 | 72,700 |
| 第 31–100 轮 | 118,458 |
| 第 100 轮以后 | 138,666 |

**第 100 轮之后的一次提问，成本是前 10 轮的 3.3 倍**，而问题本身可能一样简单。
同一账户里，13 条超过 100 轮的会话吃掉了 90.2% 的 Token。

重开一条会话把起点拉回四万左右。交接文档本身几乎不花钱——写它的时候上下文
已经在内存里，成本只是几千 Token 的输出，换回来的是后面每一轮省十万。

**但省下多少取决于交接文档写得好不好。** 如果新会话得重新读一遍文件、
重新跑一遍命令去补齐上下文，那些重新发现的成本会吃掉收益。
所以这个 skill 的重点不是"提醒重开"，是"写出一份让重开变便宜的交接文档"。

## 什么时候提议

对话轮次不是判断标准，**任务边界才是**。轮次只是提醒你去看一眼。

大约 30 轮之后，每完成一个阶段性成果时问自己：**现在切，下一个会话需要知道的
东西是不是都能写进文档？** 能，就是好的切点。

好的切点长这样：
- 一个功能做完了、测试过了
- 一份调研有结论了
- 一个决策定下来了
- 用户说"好，那接下来做 X"——话题要换了

**不要在这些时候切**：
- 正在调一个还没定位到的 bug（现场信息没法写进文档）
- 用户刚提出一个问题还没回答
- 一件事做了一半，中间状态很难描述

宁可多跑十轮到一个干净的边界，也不要在混乱处硬切——**在错误的地方切，
新会话补上下文的成本会超过省下的**。

如果一直等不到干净的边界，说明这个任务本身缺少阶段划分，
这时更该做的是先和用户一起把任务切成阶段，而不是硬拆会话。

## 交接文档写什么

写给**没有任何上下文的下一个人**。判断标准很简单：
读完这份文档，不翻聊天记录，能不能接着干？

文件名用 `HANDOFF.md`，放在项目根目录。已经有就更新它，不要新建 `HANDOFF-2.md`。

用这五节，顺序不要变——它是按"读者需要知道的先后"排的：

```markdown
# 交接文档

- 项目 / 路径 / 当前状态一句话
- 更新时间

## 一、现在在做什么
正在解决的问题是什么，为什么要解决它。
把已经想明白的结论直接写出来，不要让下一个人重新推导一遍。

## 二、已经完成了什么
做完的能力、当前的数据/配置状态、关键设计决策**及其理由**。
理由比决策本身重要——不知道为什么这么定，下一个人会把它改回去。

## 三、卡在哪里
按严重程度排。每条要写清楚：卡住的具体表现、已经排除了什么、还没验证的猜测。
"某某功能有问题"是废话，"launchd 报 Operation not permitted，
怀疑是 macOS TCC 拦了 Documents 目录，没验证过"才有用。

## 四、下一步做什么
按依赖顺序排，不是按重要性。前面没做完后面做了也白做的，要说出来。

## 五、哪些坑不要踩
**这一节通常最值钱。** 每一条都应该是真踩过的，而不是通用建议。
写清楚：做了什么、结果怎样、为什么会这样。
```

最后附一段「从零跑一遍」的可执行命令。

### 写的时候注意

**具体的数字和路径，不要概括。** "测试都过了"没用，"297 个测试全过"有用。
"配置文件已就位"没用，"`config/x.json` 有 20 条规则，但 `y` 字段还是 0"有用。

**把不确定的标出来。** 猜测就写成猜测。下一个人拿着一个假装确定的错误结论
去做决策，比什么都不知道更糟。

**只写还成立的东西。** 中途改过主意的方案不要留在文档里，
除非要说明"为什么放弃了它"——那属于第五节。

## 最关键的一步：写下一条消息

**这是决定省不省得下来的地方，但最容易被跳过。**

交接文档是给人读的，而新会话的第一条消息是给模型读的——它决定了新会话
从多高的上下文起步。写完文档后，直接给用户一段可以复制粘贴的开场白：

```
接着 <项目路径> 的工作。先读 HANDOFF.md，然后做第 N 项：<一句话说清目标>。
需要背景就看 <具体文件>，不用通读整个仓库。
```

最后那句很重要。不加限定的话，新会话往往会先把整个项目扫一遍，
一下子就把上下文推回到几万——省下来的又还回去了。

## 怎么跟用户说

说清楚三件事，然后**让用户决定**：

1. 现在是个干净的切点（说明为什么）；
2. 交接文档写好了，在哪；
3. 新会话第一条消息复制这段。

不要替用户结束当前会话，也不要在他明确说"继续"之后反复提。
提过一次就够了——真正的成本在长期习惯，不在这一次。

如果用户说不想拆，那就不拆，接着干。他可能知道一些你不知道的原因，
比如这件事马上就要收尾了。

## 常见错误

**把整段对话摘要塞进文档。** 交接文档不是聊天记录的压缩版。
下一个人不需要知道你们讨论过什么，只需要知道**结论是什么、现在在哪、下一步做什么**。
一份三千字的对话摘要，价值不如三百字的"卡在哪里"。

**只写做成了什么，不写踩过什么坑。** 坑是这份文档里唯一无法从代码里读出来的信息。
代码能告诉下一个人"现在是这样写的"，只有你能告诉他"为什么不能那样写"。

**为了拆而拆。** 一个二十轮就能做完的任务不需要交接文档。
这个 skill 的目的是降低长任务的成本，不是给每段对话都加一道仪式。
